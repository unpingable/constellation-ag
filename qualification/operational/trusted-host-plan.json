{
  "schema": "ag.operational-plan.v1",
  "qualification_status": "not_assessed",
  "environment": "trusted_host",
  "premises": ["ag.process_isolation"],
  "preconditions": [
    "Disposable or explicitly designated host with exact package and unit identities captured",
    "PID 1 is systemd and the declared namespace/LSM controls are observable",
    "No production campaign, standing, signing key, or provider credential is used",
    "Targets are disposable and all forbidden probes are read-only"
  ],
  "procedure": [
    "Run preflight.py --profile trusted-host before activation",
    "Run scripts/verify-effectd-isolation.sh against the exact release binary",
    "Run scripts/run-worker-qualification.sh with its declared safe worker fixture",
    "Capture systemctl cat/show, systemd-analyze security, process credentials, namespaces, mounts, cgroup, LSM labels, and network-denial probes",
    "Restart the exact unit and repeat identity and denial checks",
    "Seal raw stdout/stderr, source/package/unit hashes, and host boot identity without interpreting them as a claim"
  ],
  "pass_interpretation_owner": "later qualification campaign",
  "forbidden_shortcuts": [
    "Treating a source scan as live process isolation",
    "Using production credentials or effect targets",
    "Treating preflight readiness as qualification"
  ]
}
